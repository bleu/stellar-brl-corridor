# Enterprise Dashboard

Reference enterprise surface for businesses consuming the BRL/PIX corridor.

> **Status: T0 stub.** Lands in T2. React + Vite + TypeScript + Tailwind + shadcn/ui. Forkable or replaceable — this is a reference, never the product.

What it will surface:

- Onboarding via CAP-33 sponsor-sandwich (zero-XLM enterprise accounts).
- SEP-31 transactions with the SEP-38 quote and IOF disclosure inline.
- Live `quote_use` and `partner_transfer` events for reconciliation.
