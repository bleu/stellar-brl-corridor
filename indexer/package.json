{
  "name": "@bleu/stellar-brl-corridor-indexer",
  "version": "0.0.1",
  "description": "Open-source Soroban event indexer — polls Stellar RPC getEvents with cursor and writes to Postgres. T0 stub.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
