{
  "name": "@bleu/stellar-brl-corridor-sdk",
  "version": "0.0.1",
  "description": "TypeScript SDK for Bleu's BRL/PIX corridor on Stellar (Anchor Platform + SEP-38 rate-lock + partner attribution).",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc --noEmit -p .",
    "example": "tsx examples/read-live-testnet.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bleu/stellar-brl-corridor.git",
    "directory": "sdk/typescript"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.6.1"
  }
}
