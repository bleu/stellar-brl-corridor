"""bleu_corridor — Python SDK for Bleu's BRL/PIX corridor on Stellar.

T0 skeleton — version placeholder only. The client lands in T1, generated from
the deployed Soroban contract specs via ``stellar contract bindings`` plus thin
SEP-31 / SEP-38 wrappers. (The TypeScript SDK already reads live testnet state
today — see ``sdk/typescript``.)
"""

from __future__ import annotations

__version__ = "0.0.1"

__all__ = ["__version__"]
